version: "3.8"
services:
  lambdas:
    image: node:14
    container_name: lib-common-utils
    restart: always
    working_dir: /usr/src/app
    stdin_open: true
    tty: true
    volumes:
      - ./:/usr/src/app